Genmot : 
weblogo display of motifs?
Display the consensus sequence for each motif, pvalue, frequency matrix, nb of conserved sites, non-conserved sites, nb found sites per base in the background
Use a log2 base threshold

Create scangen and modify the way enhancers are defined : consider a per motif instance list.

Create imogene display:
dislay svg
display motif logo
display matrix in a human readable fashion

Compute densities with aligned background

make imogene execute a python script when generating motif logos

Marc:
- check mcmc sampling process
-Add a simple script to launch background extraction

Extract
For background : download one after the other, freeing memory in between. Be careful not to use too much memory while using background in imogene genmot...

