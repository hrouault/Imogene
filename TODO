Genmot : 

Change convention from ATCG to ACGT?
Display the consensus sequence for each motif, pvalue, frequency matrix, nb of conserved sites, non-conserved sites, nb found sites per base in the background
Display like TRANSFAC
Use a log2 base threshold

Create scangen and modify the way enhancers are defined : consider a per motif instance list.

make imogene execute a python script when generating motif logos

Marc:
- check mcmc sampling process
